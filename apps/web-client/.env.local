# این متغیرها آدرس API های بک‌اند را برای کلاینت مشخص می‌کنند.
# کلاینت (مرورگر) به شبکه داخلی داکر دسترسی ندارد و باید از localhost استفاده کند.
# پیشوند NEXT_PUBLIC_ باعث می‌شود این متغیرها در مرورگر قابل دسترس باشند.

NEXT_PUBLIC_AUTH_API_URL=http://localhost:3001
NEXT_PUBLIC_CRM_API_URL=http://localhost:3002
NEXT_PUBLIC_SALES_API_URL=http://localhost:3003
